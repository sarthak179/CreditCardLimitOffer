server:
  port: 8082
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: admin
    url: jdbc:postgresql://localhost:5432/credit_limit
    username: admin
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update